spring:
  application:
    name: Eureka
  main:
    banner-mode: 'off'

server:
  port: 8761

eureka:
  server:
    enable-self-preservation: false
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8762/eureka

